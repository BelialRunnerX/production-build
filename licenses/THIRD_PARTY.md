# Third-party dependencies

The production build fetches the pinned dependencies declared in `CMakeLists.txt`:

- raylib 5.5 — zlib/libpng license family; see the upstream project for exact bundled notices.
- EnTT 3.13.2 — MIT license.

Before public distribution, include the exact upstream license texts produced/required by the dependency build and audit all asset licenses.
